/*
 * LoadingIndicatorOverlay Messages
 *
 * This contains all the text for the LoadingIndicatorOverlay component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LoadingIndicatorOverlay';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Loading',
  },
});
