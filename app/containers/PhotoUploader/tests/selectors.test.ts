// import { selectPhotoUploaderDomain } from '../selectors';

describe('selectPhotoUploaderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
