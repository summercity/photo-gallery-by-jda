/**
 * Test sagas
 */

// import { take, call, put, select } from 'redux-saga/effects';
// import gallerySaga from '../saga';

// const generator = gallerySaga();

describe('gallerySaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
